CREATE TABLE `ciclo4`.`marcadores` (
  `MAR_ID` INT NOT NULL AUTO_INCREMENT,
  `MAR_FECHA_EVENTO` DATE NOT NULL,
  `MAR_HORA_EVENTO` TIME NOT NULL,
  `MAR_FECHA_REGISTRO` DATE NOT NULL,
  `MAR_HORA_REGISTRO` TIME NOT NULL,
  `EQUI_ID1` INT NOT NULL,
  `EQUI_ID2` INT NOT NULL,
  `MAR_EQUIPO1` INT NOT NULL,
  `MAR_EQUIPO2` INT NOT NULL,
  `DEP_ID` INT NOT NULL,
  `USU_ID` INT NOT NULL,
  PRIMARY KEY (`MAR_ID`));


CREATE TABLE `ciclo4`.`usuarios` (
  `USU_ID` INT UNSIGNED NOT NULL,
  `USU_NOMBRE` VARCHAR(45) NOT NULL,
  `USU_APELLIDO` VARCHAR(45) NOT NULL,
  `USU_EMAIL` VARCHAR(45) NOT NULL,
  `USU_CONTRASEÑA` VARCHAR(45) NOT NULL,
  PRIMARY KEY (`USU_ID`));

CREATE TABLE `ciclo4`.`equipos` (
  `EQUI_ID` INT NOT NULL AUTO_INCREMENT,
  `EQUI_NOMBRE` VARCHAR(45) NOT NULL,
  PRIMARY KEY (`EQUI_ID`));